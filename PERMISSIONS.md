# Permissions

Milestone 0 requests no runtime permissions. Future permissions are requested at point of use only: camera, foreground location, optional Bluetooth, optional microphone, and optional notifications. Background location, contacts, SMS, call logs and broad storage access are prohibited.

