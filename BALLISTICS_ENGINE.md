# Ballistics Engine

Deferred to Milestone 4. The engine will be a pure Kotlin/JVM module with no Android imports, authoritative licensed G1/G7 data, deterministic numerical integration, a root-solved zero, convergence tests, versioned traces, and no fabricated coefficients or demo result in production paths.

